import React from 'react';

import { <%= props.name %>Container } from './styles';

const <%= props.name %><%= props.tsType %> = () => {
  return (
    <<%= props.name %>Container>
      <%= props.name %> works!
    </<%= props.name %>Container>
  );
};

export default <%= props.name %>;
